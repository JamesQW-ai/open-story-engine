"""Open Story Engine Python runtime.

The story package is fixed input. Runtime state, events and derived packages are
stored separately so player co-creation never rewrites original content.
"""

__version__ = "0.1.1"
