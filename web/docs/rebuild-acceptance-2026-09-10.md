## 本轮验收目标

以 `/Users/apple/Desktop/open-story-workbench-preview.html` 的书架、开局、正文与侧栏结构为参考，重构现有前端。参考页中的模拟回复和脚本不作为产品逻辑。面向没有读过整本小说的用户提供图片、简短背景、身份说明及推荐开篇，增加深浅主题。内容方向和文学质量暂不验收。

## 实施范围

- 重写 `web/src/App.tsx`、`main.tsx`、`styles.css` 及四个既有页面。
- 新增独立 `ImportPage.tsx` 与共享展示组件 `StoryUI.tsx`。
- 删除不再使用的旧状态、上下文、JSON 等五个展示组件及旧标签文件，移除 Ant Design 依赖。
- 两张本地生成图片用于故事封面和导入场景，提示词保存在 `visual-assets.md`。
- 更新网页标题、前端 README，以及 `.gitignore` 中仅针对 `web/` 的例外，避免旧 `src/` 规则吞掉新前端。
- 沿用既有 API、数据类型及生成契约；没有改动 Python 后端或既有用户会话。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| `cd web && npm run build` | 通过，包含 TypeScript 检查；Vite 编译 38 个模块 |
| `.venv-api/bin/python -B -m unittest discover -s tests_api -q` | 39 项通过；出现既有 Starlette/httpx 弃用提示 |
| `git diff --check` | 通过 |
| 深浅主题 | 浏览器实际点击切换成功；刷新后保留浅色主题 |
| 角色与起点 | 选择身份 → 默认推荐开篇 → 创建会话成功 |
| TXT 导入 | 浏览器选择 7,723 字示例 TXT，导入后跳转到角色页，复用已有故事包 |
| 原著正文 | 原著页展示章节正文，生成分支展示自身正文 |
| 真实方向续写 | 通过网页点击方向，成功生成并显示 2,453 字符正文及下一方向 |
| 自由行动失败处理 | 提交自然语言后收到现有事实校验拒绝；原页与输入保留，显示“重试本回合” |
| 进度恢复 | 成功生成后刷新和重新打开，均恢复第 3 页 |
| 手机布局 | 在 390 × 844 断点检查书架、角色和阅读页，无横向溢出；手记抽屉宽 340 px，可打开、切到足迹并关闭 |

## 真实模型验收边界

测试 API 使用 `127.0.0.1:8001`，测试前端使用 `127.0.0.1:5176`，数据库为 `/tmp/open-story-ui-rebuild-20260910.sqlite`。保留临时测试数据库供复核，测试服务在完成后停止；正常使用入口为 `http://127.0.0.1:5173/`，连接既有 `8000` 服务。

测试会话：`189a24e6-7d29-4f5d-8535-05e6b2308006`。

成功正文：`branch_5b03ab92-95fe-44c8-b1c8-47213750b058`，`sequence=2`，`canonicalRelation=diverged`，`narrativeText` 为 2,453 字符。浏览器显示第 3 页，并更新下一方向。

第一次小方向续写因既有事实修订校验失败返回 HTTP 503；再次提交同一方向成功。自由行动“我先问姜序，接下来该如何进入站务室。”因事实问题涉及 13 段、超过 8 处局部修改上限而被拒绝，未产生第 4 页。

因此，本轮确认了“实际开局 → 方向选择 → 真实正文输出 → 保存后回读”的链路，以及自由行动提交和失败恢复界面；不代表所有输入都能生成成功，也不代表叙事质量或完整模型回归通过。没有通过放宽后端校验来制造成功结果。

## 同日第二轮：书架、身份与存档体验完善

本节替代前文的“角色与起点”界面说明，不更改此前真实模型验证的历史记录。遵循本任务的具体前端要求；不据此宣称项目其他 MVP 方向完成验收。

- 书架保留小说形式，仅展示封面、书名与“添加小说”，界面不使用“副本”称谓。
- 点击书籍 → 简短背景与图片 → 四张人物身份卡（或开篇允许的新身份）→ 第一个入口开始。取消节点选择页。
- 阅读页只保留小说、必要进度和行动；手记默认收起，打开为带键盘焦点约束的模态抽屉。开篇配一张氛围图，生成正文不强行附加无关配图。
- 新增存档删除确认、失败保留与重试，以及 play 模式删除 API。事务删除分支、评估、事件、叙述、派生故事包与审计记录；固定故事包保留。
- 文件责任：`web/src/` 页面、样式、API 客户端；`web/public/images/rainy-identities.png`；`open_story_engine/api.py`、`api_play.py`、`storage.py` 与 `tests_api/test_play_api.py` 的存档删除部分。既有生成逻辑未调整。

### 第二轮验证

- API 自动回归 45 项通过；新增覆盖关联记录清除、其他存档保留、事务回滚、缺失数据库不创建、只读模式禁止删除、故事包缺失时可删除及 CORS。测试全部使用临时数据库与故事包。
- Python 核心回归 188 项通过。
- 浏览器联调使用 `127.0.0.1:5176`、测试 API `8001`、`/tmp/story-ui-refine-20260910.sqlite`，生成采用 `MockPlanner`。会话 `15d641ed-9d76-4a41-b821-8216a39a5deb` 以唐栖从首入口开始；选择方向与自由行动均产生正文，重新进入恢复第 3 页。
- 浏览器验证了删除弹窗与取消后进度保留；实际删除及其事务一致性由临时数据库 API 自动测试覆盖，未在用户存档上点击删除。
- 390 × 844 手机检查：浅色书架、添加小说入口、身份卡、手记抽屉均可用；阅读页面宽度与滚动宽度均为 390 px，抽屉宽 351 px。
- 从添加小说入口选择已有 7,723 字示例 TXT，提交后复用原故事包并进入背景简介，然后进入身份选择。
- 本轮未执行新的真实模型质量验收，模拟正文只证明交互与保存链路；上一轮真实模型边界仍见前文。
- 最终 `npm --prefix web run build`（含 TypeScript 检查）与 `git diff --check` 通过，浏览器错误日志为空。
- 正常 API `8000` 已重启加载删除路由，健康检查为 `play` 且可生成；重启前后用户数据库 SHA-256 相同，仍为 4 份会话、5 条分支。

## 同日第三轮：新手引导、具体方向与真实流式正文

本节记录后续界面与生成链路调整，前面的数字与模型配置仅代表对应历史轮次。

- 书架增加三步简短引导，保留小说与添加入口；清除前端按钮和链接中的装饰箭头。
- 《雨夜候车室》序幕使用 42 字背景悬念；其他小说使用已有简介并控制在 50 个 Unicode 字符以内。身份页标题为“请选择你的身份”，四张人物卡补充处境和动机。
- 方向按钮同时展示标题与行动说明。过滤空泛占位项，菜单不足两个时补充可提交的自由行动，不虚构方向 ID 或直接修改故事状态。
- 前端新增 SSE 客户端；API 新增流式回合路径，将真实模型片段直接送达页面。完整结果通过现有本地校验后才记为新存档页；异常预览保留在可展开区域，允许原请求重试。
- Web 使用简短正文提示、不要求最低字数、不额外调用模型审阅原著事实；保留本地事实和状态约束。CLI 默认行为不变。同一次自由行动已完成判定但正文失败时，重试恢复正文生成，不重复登记行动。

### 第三轮验证

| 检查 | 结果 |
| --- | --- |
| API 回归 | 49 项通过；新增流式片段、reset、完成保存、错误、同请求去重、失败恢复、未知存档、只读模式覆盖 |
| 核心回归 | 188 项通过 |
| 前端 `npm test` | 5 项通过；中文 UTF-8 单字节切分、流结束与失败、简介上限、至少两个方向 |
| `npm run build` | TypeScript 与 Vite 构建通过，39 个模块 |
| `git diff --check` | 通过 |
| 手机与主题 | 390 × 844 书架、序幕、身份卡、阅读页无横向溢出；检查浅色手机和深色桌面身份卡，测试后恢复窗口尺寸 |
| 装饰箭头 | `web/src` 中方向箭头字符及旧标题“你是谁？”无匹配 |

真实模型测试使用独立 API `8001`、前端 `5176`、数据库 `/tmp/story-live-stream-20260910.sqlite`，没有向用户数据库写入测试回合。

- HTTP 实际流：会话 `d80467bd-2d19-46aa-9ad8-bc3ee814fd39`，1.943 秒收到首个正文片段，4.546 秒收到 `done`，155 个片段、480 字符已保存正文。事件时间证据位于 `/tmp/story-live-stream-evidence.json`。
- 浏览器实际操作：选择许川并开篇，点击具体行动，1.636 秒内观察到生成中的 26 字符正文且行动按钮禁用；随后完成保存并显示第 2 页。会话 `66e00e1b-8684-4580-b42d-02f9c450b893` 的生成分支 `branch_0e996e19-dc35-48b3-baef-f28390f6aa78` 保存 582 字符，重新进入后恢复正文和两个具体方向。
- 正常 API `8000` 已按 `STORY_LLM_STREAM=true` 重启，新路由可用，健康检查为 `play`、可生成。重启前后数据库 SHA-256 均为 `32cf83b3da9400fd0f6b90b4848b2bd7ba3575cb5926fd0f13aa48ce5d18ba22`，1 份用户会话、2 条分支保持不变。

本轮证明真实流式展示、方向提交、保存回读和受控失败重试可运行，不代表文学质量验收或任意行动均能成功。断开浏览器连接后当前进程中的生成可继续保存；尚无跨进程任务恢复和事件历史回放。

## 同日第四轮：身份切入点与长文多图阅读

- 新开局提交 `identity_opening: true`。《雨夜候车室》根据源文本摘要校验绑定四份开场：许川“十九秒之后”（候车厅）、唐栖“门的另一侧”（信号室）、陈砚“放行之前”（站台）、姜序“未说完的回答”（候车厅）。正文、摘要、待处理问题和首轮行动均按身份保存；角色位置与会话初始状态一致。
- 阅读页修复身份开场被整章原文覆盖的问题。其他小说优先选择该角色主导的最早声明入口，开场来自其自身角色资料和原文片段；没有匹配的独立入口时回退到已有可用入口。
- 长文在约 600 字后的段落边界分节，超长单段再按句子拆分；不删减正文，不重新排列已输出内容。增加三张本地场景插图，长文懒加载并预留尺寸。身份短开场只保留一张对应场景图。
- 文件责任：`api_openings.py`、创建请求与服务、核心入口状态/正文的可选字段；前端身份入口选择、`StoryProse.tsx`、`readingLayout.ts`、阅读页和样式；相关 API 与前端测试、图片和说明。
- 原始故事包字节与用户已有存档均不迁移。可选身份开场默认关闭，原 API 与 CLI 的默认开局行为保持不变。

### 第四轮验证

- API 51 项、Python 核心 188 项、前端 9 项测试通过；TypeScript 与 Vite 构建通过（41 个模块），`git diff --check` 通过。
- 新增测试覆盖四身份开场、正文与摘要差异、合法角色位置、初始状态一致、回读、各自首个行动能够续写、原故事包无改写、不同小说不套用示例人物、角色主导入口排序、长文无丢字及流式分段稳定。
- 浏览器以唐栖从信号室开局，确认看到专属正文、标题、插图和两个首轮行动。点击“查看门边和设备旁的环境”后，真实模型返回并保存 538 字符正文，继续采用唐栖视角且玩家仍在信号室。
- 隔离验证库为 `/tmp/story-identity-reading-20260910.sqlite`；浏览器验证会话 `982139d6-02c5-4b30-b6a5-e653447d3df4`，生成分支 `branch_25f2fce0-d841-4ea4-a608-8ca067c65369`。此次浏览器抽查确认成功结果，未重新测量首片段延迟。
- 完整原著长页 `ce7303c8-6ca7-4a15-9e92-03d55e20e325` 展示 1,878 字符正文、三段和三张插图。桌面检查段落衔接；390 × 844 手机页面滚动宽度为 390 px，无横向溢出，检查后恢复窗口尺寸。
- 正常 API `8000` 已加载新开场配置；重启验证时原数据库仍为 3 份会话、4 条分支，SHA-256 `718eab1d9274e485d08d6301e68e9ebec8db8143dd56236248662d28f0333ca5` 未变。

示例小说的开场为依据原文设计的固定角色视角；其他小说目前使用资料与入口原文组合，尚未逐部设计同等细致的专属开场。插图目前从本地图集中匹配，不调用模型逐回合绘制。旧存档保留原开场，新体验需从书架重新选择身份。后续功能建议仅讨论，未追加实现。
