状态：P0 完成。验收对象为 `taixu-relics-part1@0.1.2`；只涵盖长篇包完整性、官方人物开局与上下文边界，不代表整本小说的持续生成已经验收。

## 交付内容

- 同一冻结母本、已补齐事件的 analysis、开局审核配置统一编译出 package、reader、modules、索引与审计报告。新版本保留 50 章、343 个剧情锚点、7 张人物卡、3 个可玩入口，共 670 个模块。
- 惰性加载索引保留审核后的 `defaultEntryPointId`、`identitySummary`、`roleGroup`、肖像及可玩状态，解决网页与完整包字段不一致。
- 前端优先消费角色默认入口；CLI 选择人物后自动进入；服务端使用相同解析器，即使请求旧的任意角色入口模式，也拒绝官方包的错配、NPC 扮演和自由新建人物。
- 开局状态独立记录人物位置、已持有物、放在场景中的物品；不再继承整章推断的物品归属。未登记伤者手边的空灯保留为明确场景事实，不错误授予玩家。
- 开局及首次行动上下文使用审核过的限知材料。开场生成不接收全书人物简介，也不将用于追溯的早期原文持有状态覆盖当前快照。
- 模型开场须通过第二人称、基础事实与独立的开局事实复核。失败最多改写一次，仍失败则不创建正式开局；成功记录 `openingGeneration`。开场流式内容在复核前只是预览。
- 契约按原文段落截止点区分既有历史与可变未来。`characterOutcomeStates` 已随分支初始化、保存和恢复，并声明未来记录格式；P0 锁定为空，P3 才启用死亡、永久离队及后续影响，不将字段预留冒充功能实现。
- 产品文档和项目指令统一为官方内容、未知读者、自动入场、禁上传、桌面端。序幕同步去掉母本没有的“攥住衣角”等细节。

## 来源与复现

母本：`docs/太虚遗录-第一部/太虚遗录-第一部-合并.txt`。按 `[\u3400-\u4dbf\u4e00-\u9fff]` 计数 102610 个汉字，SHA-256 为 `61efaffb17d652ccc0fcb15b98a133fd976296d342966c0a426b7831f9810904`。

审核输入：[official-openings-v1.json](../content/authoring/taixu-relics-part1/official-openings-v1.json)。该配置给出逐角色原文证据、唯一截止句、前史、关系、未知边界、物品位置和开局正文。它是可审阅的作者侧配置；不是模型自行赋权的状态补丁。

复现到独立目录，避免覆盖已有运行版本：

```bash
cd /Users/James/open-story-engine
python3 -B -m open_story_engine build-story-package \
  docs/太虚遗录-第一部/太虚遗录-第一部-合并.txt \
  content/packages/taixu-relics-part1/0.1.2/analysis.json \
  --opening-review content/authoring/taixu-relics-part1/official-openings-v1.json \
  --id taixu-relics-part1 --version 0.1.2 \
  --output /tmp/taixu-p0-rebuild/taixu-relics-part1/0.1.2/package.json
```

最终编译器复现的 package 内容与发布文件一致（模块 hash 由模块编译阶段生成）；模块审计验证所有内容、路径和哈希。旧版本与用户原有存档保留。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| package 与模块审计 | 均 passed；reader 50 章、beat 343、入口 3、模块 670 |
| 核心回归 | 197 项通过 |
| API 回归 | 77 项通过；临时数据库 |
| 前端测试与构建 | 11 项通过，TypeScript/Vite 构建通过 |
| 差异检查 | `git diff --check` 通过 |
| 入口权限 | 三角色互换入口被拒绝；NPC 和自由角色被拒绝；CLI 只问一次身份 |
| 状态与证据 | 存档恢复一致；篡改模块、错配人物/物品、越过截止点的证据被检测 |
| 本地 HTTP 上下文 | 三个真实会话的 `/api/v1/context/build` 均成功，使用开局限知材料 |
| 桌面真实模型 | 三角色分别选择身份、生成、通过复核、保存并进入行动页；没有二次选场景 |

模型配置与实际保存正文、会话/分支 ID、入口、状态及复核结果见 [live-openings.json](evidence/p0-2026-09-15/live-openings.json)。这是通过网页调用实际服务得到的三份开场，不是 MockPlanner 回放。浏览器验收在桌面窗口进行，没有新增手机端样式或测试。

| 角色 | 当前场景与关键核对 | 正文抽查结论 |
| --- | --- | --- |
| 陆照临 | 山门封山线外；文书仍在手中；空灯归伤者；父亲失踪原因未知 | 保留寻父前史与救人冲突，没有替玩家救人、接灯或跨线 |
| 顾长离 | 第三章试炼开始前；持木牌、包金屑的纸；刚认识陆照临 | 没有把陆前两章的经历写成她的记忆，没有提前获得账单、灵石或共同调查关系 |
| 叶观澜 | 第七章雨廊对质；无芯灯与铜环已放下；认识沈执事 | 使用眼前年轻人称呼尚未当面介绍者，保留日期矛盾，不泄漏残印、拘押或后续去向 |

## 真实试玩发现与剩余边界

初次扩写出现过空灯滚动、青光位置变化和文书回到怀里的偏差；随后增加事实复核并移除提示中易混淆的早期状态引文。最后保存的三份样本经过模型复核与逐段检查。失败与重试真实发生过，不能将最终三个成功样本表述成首次请求成功率 100%。原有调试样本不构成验收证据。

本次没有生成图像来验收文本；测试期间只对服务进程临时关闭生图模型，未修改 `.env`。正式服务随后恢复原配置。配图复用、排队取消与成本验收属于 P2。

固定开场的模型输出接近审核正文，这符合 P0 的事实保持目标，不能证明开放行动、长期分支或整书文笔。开场尚有生成、复核和重试延迟，P1 需要单独测量与优化。

母本正文未在本轮重写。部分章节尾部存在重复/程式化语句，应在作者侧另行文学审校；包完整性通过不表示全书文学质量通过。P1 之后仍须按 P3/P4 完成永久后果、持续偏离、伏笔回收与完整终局验证。

下一阶段：P1，每个可见预设方向出现时预生成对应下一回合正文，并保证未选草稿不进入正式分支；自定义行动提交后优先生成。
