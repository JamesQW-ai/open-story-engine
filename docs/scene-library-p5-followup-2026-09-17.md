P5 素材与图片兼容性复核

## 结果

对当前唯一达标官方长篇《太虚遗录》第一部（102610 CJK，`taixu-relics-part1@0.1.2`）执行只读 `check_scene_library`：

- 清单状态 `valid`，错误数 `0`；
- 已登记素材 3 项，检查 3 项；
- 实时规则 1 条，检查 1 条；
- 版本、源文件绑定、文件存在性和规则结构均通过。

兼容性专项 39 项通过，覆盖人物存活／死亡／离队／失踪／受伤、道具永久损毁、跨分支状态和未确认状态。已审校素材继续优先复用，未新增图片、未调用生图、未改正式存档。

## 边界

清单检查只证明登记和兼容性程序契约，不证明图片内容审校、全书名场面覆盖或实时生图质量。图片展示延迟和成本统计沿用已有记录；END-001、NQ-001、SRC-001 继续暂缓。
