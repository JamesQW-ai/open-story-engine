P4 桌面故事手记集成核查

## 范围

使用桌面阅读夹具导出当前唯一达标官方长篇《太虚遗录》第一部（102610 CJK，`taixu-relics-part1@0.1.2`）。夹具只加载真实长篇章节和开局状态，不调用模型，不写正式存档。

## 核查结果

- 夹具导出成功，50 个章节、源文件 SHA-256 与长篇清单一致；
- `health`、会话、根分支手记接口可读，根分支保持 `progress: null` 和“沿途经历”，没有伪造百分比；
- 注入兄弟分支后，目标、剧情问题、人物永久死亡状态只在对应分支显示；
- 目标页能区分旧目标 `abandoned` 与当前 `active` 目标；
- 剧情问题页能同时展示 `resolved`、`abandoned`、`open` 及正文依据；
- 夹具服务已正常停止，未留下运行进程。

## 边界

本轮为桌面 API/夹具集成核查，前端自动化回归仍以 45 项测试和生产构建为准；未扩展手机端、未修改手记数据投影、未调用真实模型。P4 的完整视觉人工验收和关系图交互仍需在桌面浏览器中抽样，不把本轮接口核查写成完整视觉验收。
