# MVP 验收标准

## 当前产品验收：官方长篇小说互动阅读

2026-09-15 确认，唯一产品方向 面向第一次接触官方小说的用户，验证“官方小说 -> 未知读者开局 -> 方向选择或自由文本 -> 持续阅读、分支和收束”。用户上传、已读原著节点介入和从零生成不属于当前产品入口。

官方维护的版本化 `StoryPackage` 是唯一故事来源；用户只产生本局私有分支。

### 身份与入口约束

- 用户不能上传小说或替换故事包内容。
- 可玩身份须经过审核，并按 `roleGroup` 分组；关键 NPC 可显示公开简介，但须标为暂不可扮演。
- 当前只开发与验收桌面端。
- 角色简介、关系、初始状态和默认入口必须来自同一版本的 `StoryPackage`。
- 选择角色后自动进入该角色的 `defaultEntryPointId`，不得再次要求选择初始剧情节点。
- 当前 MVP 不支持自由创建角色；词条化自定义角色属于后续范围。

入口必须提供足够的背景和可读、连贯的正文；用户不需要预先读过任何原著。

### 范围与必过场景

| 场景 | 操作 | 通过标准 |
| --- | --- | --- |
| 未知读者开局 | 从官方书库选择一部小说并开始阅读 | 可理解人物、当前处境、必要前史和当前目标；开局固定官方包版本与知识边界。 |
| 官方角色与设定 | 使用官方小说中的人物和世界规则开局 | 玩家身份、人物行为和世界事实一致；不把角色尚未知晓的后续情节提前写入认知。 |
| 角色选择与自动入口 | 按分组选择一个关键角色并确认开始 | 可扮演与仅展示角色区分明确；简介和关系与角色一致；确认后自动进入默认入口，不出现第二个初始剧情选择。 |
| 主动改写 | 选择一条偏离原著的方向，并覆盖自由文本输入 | 结果体现用户意图与规则允许的后果，形成可追溯分支，不强制回到原著；不支持的输入有明确说明且不误提交。 |
| 持续互动 | 在同一分支至少完成三个连续回合 | 正文承接已确认行动、人物关系、状态和未解事项；方向选择与自由文本都有实际后果。 |
| 分叉与续玩 | 从同一父节点形成两个分支，退出后重新读取并继续其中一个 | 父节点历史不被覆盖，分支互不污染，角色、方向、正文与状态可正确恢复并继续。 |
| 来源与状态边界 | 对比原著包、会话契约、已确认状态和生成正文 | 原著包保持不变；生成内容不将未确认事实或能力当作既成事实；状态变更经本地规则校验并可追溯。 |
| 失败恢复 | 注入模型超时、无效输出或事实冲突后重试 | 未采纳草稿不写为正式剧情，不损坏分支；错误可识别，同一逻辑请求不重复提交。 |

### 长篇专项必过场景

| 场景 | 通过标准 |
| --- | --- |
| 正文预生成 | 每个可见预设方向均启动下一回合正文准备；选中 ready 草稿无需再生成，未完成任务复用，未选草稿不写正式状态 |
| 自定义方向 | 提交后生成；复合意图逐项兑现或明确解释实际铁律冲突，不能用无关行动替代 |
| 永久后果 | 主要人物永久退出后，正文、状态、后续事件与插图均保持一致，保存恢复后不复活或无故归队 |
| 名场面复用 | 条件兼容时跨会话复用预制素材，条件不兼容时不展示错误人物/道具；排队任务可随订阅取消 |
| 长篇运行基础 | 包、reader、modules、索引来自同一冻结母本并通过完整性校验；不能只有 package.json |
| 持续偏离与收尾 | 一条严重偏离路线能完整结束；关键伏笔均有正文中的解决、转化或放下依据，不强拼原著、不突然终止 |
| 性能与成本 | 分别记录预生成命中、首字、正文完成、图片显示及未采用成本；以真实模型和桌面端证据验收 |

具体阶段和证据要求见 [后续开发计划 2026-09-15](next-development-plan-2026-09-15.md)。这些条目是待验证门槛，不是完成声明。

### 完成门槛与证据

1. 上述场景均有对应证据，注明故事包 ID/版本、入口、角色、输入步骤、会话/分支引用、预期与实际结果。
2. 确定性回归覆盖契约、状态、分支隔离、持久化和失败路径；保留下文早期 CLI 基础回归的适用检查。
3. 使用真实模型完成主动改写与至少三个连续回合的正文验证；`mock` 只证明结构和规则，不证明共创正文体验。真实模型专项运行遵循 [真实 LLM 评估 v0.1](live-llm-evaluation-v0.1.md)，既有专项结果不能自动替代本表的产品场景证据。
4. 人工试玩以完全未知原著的玩家视角开展，检查入口可理解性、角色代入、选择后果和持续可读性。每项判断附具体回合及问题，不能只写“文笔不错”。
5. 记录实际验收入口（CLI/API/玩家端）及其能力边界；底层接口通过不能自动视为玩家端流程通过。

当前状态：P0 长篇运行基础与三个官方角色开局已完成专项验收，见 [P0 验收记录](p0-acceptance-2026-09-15.md)。P1 的正文预生成、草稿提交与完整行动核对已通过专项回归和真实模型/桌面抽样，见 [P1 验收记录](p1-acceptance-2026-09-15.md)，P95 目标仍待扩大测量。P2 图片复用与任务控制、首批开局环境图已交付，范围见 [P2 验收记录](p2-acceptance-2026-09-15.md)。完整 MVP 尚未通过：全书名场面素材、P3/P4、持续互动与严重偏离后的收尾仍待验证。

2026-09-16 排期调整：叙事质量及生成/审查/修复稳定性已登记为 [NQ-001](issues/2026-09-16-narrative-quality-deferred.md)，状态为未解决、暂缓集中攻坚。允许先完成桌面阅读、测量、任务生命周期和状态隔离等独立工程优化；这些专项通过不关闭 NQ-001，也不替代真实连续回合和未知原著视角试玩。恢复攻坚时间与关闭条件见问题记录，完整 MVP 仍未通过。

## 早期 CLI 基础回归（历史范围）

以下保留首个固定故事切片的技术验收标准。“范围”“完成门槛”和真实 LLM 附加检查仅指这一历史切片，仅用于底层技术回归，不恢复任何历史产品方向，也不替代上面的玩家端产品验收。

### 验收对象

本标准只适用于 `rainy-waiting-room` 的单机 CLI MVP。验收目标不是证明模型文笔，而是证明互动叙事引擎能让一个预置故事包稳定地完成一局可追溯的游戏。

### 范围

必须交付：

- 加载并校验一个 `StoryPackage v0.1`。
- 创建、读取和继续一个本地游戏会话。
- 接收自然语言玩家输入，并归类为 `investigate`、`negotiate` 或 `risk`。
- 由代码执行前置条件、随机检定、状态变化和节点转移。
- 记录不可变事件与可验证的当前状态快照。
- 使用 `mock` 叙事器完整试玩；真实 LLM 适配器作为受控的附加验证。
- 通过 `status` 查看对玩家有意义的当前目标、地点、背包、关系与已知事实。

不在范围内：图形界面、任意小说解析、图片、用户账号、联网同步、多人故事、共创模式和自动生成故事包。

### 必过场景

| 场景 | 操作 | 通过标准 |
| --- | --- | --- |
| 内容校验 | 加载合法包与缺少引用的非法包 | 合法包可启动；非法包在建档前被拒绝并指出引用错误。 |
| 新建会话 | 以默认方向启动《雨夜候车室》 | 初始状态与故事包模板一致，首个事件和会话 ID 可查询。 |
| 调查 | 在候车厅寻找线索 | 仅在规则允许时取得线索或物品；状态差异写入一个事件。 |
| 交涉 | 与姜序或陈砚交流 | 关系、通行资格或压力变化由检定决定，不由叙事文本决定。 |
| 冒险 | 尝试绕过封锁或进入隧道 | 未满足前置条件时拒绝状态变更；满足条件后执行确定的风险结果。 |
| 非法输入 | 空输入、无法解析输入或不可达行动 | 不写入新的游戏状态；向玩家返回可继续操作的说明。 |
| 状态重建 | 从事件序列重放会话 | 得到与当前快照完全相同的版本化状态。 |
| 结局 | 覆盖三个已定义结局条件 | 每个结局只由可持久化状态触发，且终局后不再接受普通行动。 |
| 叙事隔离 | 使用不同 `Narrator` 实现重放同一已判定回合 | 检定结果、状态差异和节点转移不变；仅正文可不同。 |

### 不变量

- 一次已提交事件只能追加，不能被后续回合修改。
- 所有运行期实体引用均使用稳定 ID，不能通过显示名称匹配。
- 每次状态写入必须伴随对应的事件记录，并处于同一 SQLite 事务中。
- `RuleEngine` 是唯一允许产生 `StateDelta` 的模块。
- LLM 结构化输出未通过 schema 校验时，不得推进回合。
- 默认随机源必须可注入并支持固定 seed，以便测试和问题复现。
- 任一非终局状态至少存在一个可行的规则路径；模型不得靠自由收尾掩盖死路。

### 完成门槛

满足以下全部条件才可进入真实 LLM 调优或 Web 界面开发：

1. 自动化测试覆盖上述必过场景，且本地全部通过。
2. 使用 `mock` 叙事器可以从新建会话走到三个结局。
3. 从任意测试检查点重放事件可重建相同状态。
4. 人工试玩至少完成一条成功路径和一条受控失败路径；两者都能继续阅读、理解当前目标或明确结束。
5. `status` 输出与数据库中的快照和最后事件一致。
6. 不依赖 LLM 也能验证故事包和执行全部规则。

### 真实 LLM 附加检查

真实 LLM 不改变上述完成门槛。启用后还必须验证：

- 解析器输出只使用约定的行动类型、实体 ID 和字段。
- 叙事器不声称尚未发生的状态变化，不替玩家补写行动意图。
- 单回合调用次数、输入上下文和输出长度均受配置上限控制。
- 模型调用失败时，已提交的规则结果仍可用可恢复标记重新叙事，而不会损坏存档。
