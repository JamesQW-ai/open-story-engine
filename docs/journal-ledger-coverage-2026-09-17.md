2026-09-17 P4 手记账本覆盖状态：目标页和剧情问题页新增 `ledger_coverage`，区分当前分支账本已记录（`recorded`）与缺失／格式无效（`unknown`）。保留公开开局目标和问题列表的原有投影，但在状态未确认时明确提示，不能把空列表、人物缺席或旧菜单消失解释为目标完成或问题解决。

验证覆盖：官方长篇运行包《太虚遗录》第一部（102610 CJK，`taixu-relics-part1@0.1.2`）的有效账本、缺失账本和非法账本；兄弟分支的状态仍只来自当前祖先链。桌面目标页和剧情问题页分别显示 unknown 提示，正常账本不增加提示。

验证：`431` 项 API、`25` 项 core、`43` 项前端测试、生产构建及 `git diff --check` 通过。未调用真实模型，未改正式存档；NQ-001、END-001、SRC-001 继续暂缓。
