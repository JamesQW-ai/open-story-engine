路线监测新增 `source_progress_stalled` 信号：连续三个已提交回合保持同一真实 `sourceProgress` 时，标记需要复核，即使回合只改变了元数据或无关字段。缺少 `sourceProgress` 不产生该信号，等待不会自动结束路线，信号只用于收束与停滞诊断。

测试覆盖《太虚遗录》第一部全部身份夹具，确认正常重复等待会同时保留原有状态／行动／正文信号，元数据变化不能掩盖原著进度停滞。该检查不调用真实模型、不改变状态，也不把诊断当作叙事质量结论。
