本批执行 P6 真实模型终局判断专项，结果为 **5/7 符合预期，整体未通过**。代码接口和桌面流程可运行，不能据此宣称正常／偏离／失败三类自然终局均稳定可用。

## 范围与方法

自动盘点所有十万汉字以上官方母本，当前只有《太虚遗录》第一部（102610 CJK，`taixu-relics-part1@0.1.2`）。没有执行退役短篇。

新增 `test_support/live_endings.py` 与 `test_support/fixtures/ending-review-taixu.json`。每个样本独立创建临时会话，沿用长篇开局身份、目标和问题，再显式注入人工对照正文及账本，调用真实 `propose_ending` 入口。正例用于检查类型支持，反例故意让形式上清空的账本与正文语义不一致，以确认独立审查没有只看清单放行。

夹具中的死亡、道具损坏或父亲去向只属于隔离测试，不是原著新增事实，也没有写入正式玩家存档。它们未经过完整生成、事实审查及连续游玩，不作为叙事质量或长篇终局效果证据。

每个样本最多一次供应商请求，固定普通 JSON、45 秒超时、关闭本次评估的传输回退；不写作、不修复、不自动重试。本次使用现有配置的 `deepseek-v4-pro:cloud`，未修改产品默认网关设置或终局提示。报告记录当前提示版本、母本摘要、完整模型输入、原始响应、检查理由与实际用量，不包含密钥和供应商地址。

## 结果

| 样本 | 预期 | 模型结论 | 判定 |
| --- | --- | --- | --- |
| 正常：救助与空灯关系有明确结果 | allow | allow | 通过 |
| 偏离：主动放下目标并完成退场 | allow | unknown | 未通过 |
| 失败：不可逆失败且善后结束 | allow | reject | 未通过 |
| 空泛宣布结束，无目标结果 | 拦截 | reject | 通过 |
| 未来打算冒充已完成 | 拦截 | reject | 通过 |
| 主动放弃冒充失败结局 | 拦截 | reject | 通过 |
| 旧目标完成但新增危险未解决 | 拦截 | reject | 通过 |

四个反例都未获批准，没有出现本批样本中的错误放行。两个正例未通过已登记为 [END-001](issues/2026-09-17-ending-review-false-rejection.md)，不通过修改预期或反复重试消除失败记录。模型对空泛证据选择 `reject`，因此本批不证明它能稳定区分 `unknown` 与 `reject`。

所有样本均验证审查后路线仍为 `active`、重复提案复用原记录且不新增模型调用。正常正例进一步验证显式提交、重复提交返回同一回执，以及结束后拒绝普通续写；四个反例进一步验证显式提交被拒绝。两个未通过的正例在预期断言处停止，不能声称本轮对它们验证了提交环节。

7 次供应商请求全部返回有效审查结构，无传输／格式失败。供应商共报告 **13,393 tokens**，未报告用量次数为 0；模型审查耗时合计 **27.13 秒**，单次 **2.731～6.519 秒**。这是一轮样本数据，不是长期成功率、稳定延迟或货币费用估算。

证据：[report.json](evidence/live-ending-review-2026-09-17/report.json)。其中 `status=completed` 表示测试运行结束，`passed=false` 表示未通过；脚本退出码为 1。

## 复现与工程检查

```bash
.venv-api/bin/python -B -m test_support.live_endings --dry-run --output /tmp/ending-review-dry-new
.venv-api/bin/python -B -m test_support.live_endings --output /tmp/ending-review-live-new --max-calls 7
```

输出目录必须不存在，以免覆盖先前证据。缺少任一达标母本的样本或总调用上限不足时，在模型调用前拒绝启动；新增长篇需补充对应的人工样本。`--dry-run` 只检查长篇、身份、目标、引文与必要账本条件，零供应商调用，不算模型验收。

- 七个样本的离线预检全部通过，0 次供应商请求。
- `.venv-api/bin/python -B -m unittest tests_api.test_longform_ending_proposals`：15 项通过。
- `git diff --check`：通过。
- 本轮只新增验证工具、样本和记录；未修改产品运行逻辑、提示词或前端，未重新运行完整 API／核心／前端回归，也未进行新的桌面试玩。

下一步可独立核查终局审查的中断恢复、待处理记录和真实请求用量口径。END-001 与 NQ-001 保留待集中处理；完整自然终局、卷／Arc 编排及 SRC-001 仍未验收。
