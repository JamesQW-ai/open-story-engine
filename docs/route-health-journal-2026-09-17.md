2026-09-17 P6 路线停滞手记展示：故事手记的 `route_health` 现在返回真实 `source_progress_streak`，目标页在已有 `source_progress_stalled` 信号出现时提示连续未推进的回合数。提示只反映路线诊断，不修改状态、不自动结束，也不把缺失 `sourceProgress` 推断成停滞。

验证覆盖：API 增加 1 项 `route_health` 回归；桌面前端类型和目标页构建通过。继续使用当前唯一达标官方长篇《太虚遗录》第一部，未使用《雨夜候车室》，未调用真实模型，未修改正文、故事包或生成流程。
