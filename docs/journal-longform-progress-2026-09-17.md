2026-09-17 P4 长篇路线进度：手记进度改为消费运行包叙事图谱中连续的 `sourceProgress` 与 ending beat。仅当进度从 `chapter_001` 连续覆盖到结局锚点时计算百分比；开局为 0%，结局为 100%，中间按真实节点位置计算。缺少连续分母、不是源路线或无法确认当前节点时，继续显示本路线选择数，不用回合数、正文长度或模型判断伪造百分比。

当前正式清单只有《太虚遗录》第一部（102610 CJK，`taixu-relics-part1@0.1.2`）。该运行包提供 `chapter_001` 至 `chapter_343`，ending beat 为 `beat_chapter_343`；长篇收束回归确认开局显示 `0` 与“原著路线进度”，终局仍显示 `100` 并进入 `completed`。小型图谱夹具覆盖 0/50/100 及缺少分母时保持 `null`。

验证：`430` 项 API、`25` 项 core 通过，`git diff --check` 通过；前端本轮未改，沿用本轮前 `43` 项测试和生产构建通过结果。未调用真实模型；NQ-001、END-001、SRC-001 继续暂缓。
