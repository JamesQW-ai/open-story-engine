2026-09-17 P6 原著进度回退诊断：路线监测按运行包 `narrativeGraph.beats` 的首次声明顺序记录 `sourceProgress`。已知进度回到更早 Beat 时新增 `source_progress_regressed`，手记显示需要复核当前分支；运行包没有可验证顺序或进度未知时不生成该信号。

验证覆盖：新增 2 项官方长篇路线监测回归和 1 项前端文案回归。信号只读，不回滚状态、不自动结束路线，也不把回退本身认定为叙事错误或结局。
