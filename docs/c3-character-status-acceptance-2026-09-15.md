状态：C 第 3 步已完成状态展示与只读桌面专项验证。未新增真实模型生成，不代替完整长篇试玩验收。

## 本次实现

- 关系图和详细身份卡共用当前分支的人物状态：死亡头像灰度并降低亮度，保留姓名和可点击详情；离队保持彩色，头像边框改为虚线。
- 关系图给已确认状态显示文字，辅助技术可读取状态说明。未确认状态只在详细卡明确标注，避免图谱堆叠重复提示。
- 详细卡显示状态及公开正文依据、来源页；离队区分永久与暂时。沿用现有角色肖像和预设头像，不调用生图。
- 状态来自 C1 的正式分支投影；不从缺席、猜测、简介或未来剧情推断。切换分支沿用清空人物卡、丢弃过期人物概况请求的机制。

## 验证结果

前端 14 项测试、人物展示数据 7 项测试、TypeScript 与 Vite 构建、`git diff --check` 通过。

使用只读 API（8001，`STORY_API_PLAY=0`）与临时桌面前端（5174）检查既有会话 `22b9da04-7a01-5489-a7c4-682e88fab80d`，不触发方向预生成或人物概况生成。

| 已保存页面 | 实际展示与检查结果 |
| --- | --- |
| 第 3 页，陆照临死亡 | 图谱与人物卡头像灰度；键盘 Enter 可以打开详情；依据标明第 3 页 |
| 第 36 页，死亡后的路线 | 继续显示死亡，依据仍为第 3 页的原始记录 |
| 第 26 页，永久离队 | 保留彩色，虚线头像框；详情显示永久离队与第 26 页原文 |
| 第 32 页，另一条路线 | 状态未确认，无灰度、无离队边框、无死亡引文 |
| 第 1 页，故事开篇 | 状态未确认，不提前泄漏未来死亡或离队 |
| 第 40 页，顾长离 | 存活，保留彩色头像，显示本分支依据 |

死亡图谱与身份卡完成桌面深浅主题检查。界面取样见 [分支检查记录](evidence/c3-character-status-2026-09-15/branch-ui-checks.json)、[浅色死亡卡](evidence/c3-character-status-2026-09-15/death-card-light.png)、[深色图谱](evidence/c3-character-status-2026-09-15/death-graph-dark.png)、[离队卡](evidence/c3-character-status-2026-09-15/departed-card-light.png)。

测试前后 `data/open-story-engine.sqlite` SHA-256 均为 `3ba2695159f454572bd5c93b27e225a85bb0df8ec8537d1626daab2e5e740eaa`。临时只读服务与验收标签关闭；原开发服务保留。

## 边界与后续

`missing` 保留协议与虚线样式支持，但上游尚未提供经验证的失踪事件；本次未宣称失踪端到端验收。伤势等细分状态不由当前结局字段推断。临时离队样式可消费既有字段，真实存档抽样仅覆盖永久离队。

变更限于 `CharacterGraph.tsx`、`SessionPage.tsx`、`styles.css` 和验收文档。没有更改生成提示词、后端状态判定、小说、故事包或手机端布局。下一步先做 C 的桌面整体联调，再进入 D 的方向数量与篇幅节奏优化；完整长篇终局继续单独验收。
