## 范围与结果

完成 C 第 2 步：人物关系图使用已有角色肖像与姓名。玩家保留金色边框和“你”标记；点击人物仍打开原有人物卡。新增 8 种简洁的预设头像，用稳定编号区分没有单独肖像的人物，不从名字推断性别、年龄或阵营。

图片尚未加载时，预设头像先在固定圆框内显示；图片成功后覆盖，加载失败则移除失效图片并保留兜底。更换图片 URL 后可显示新图片，清空引用时恢复预设头像。不同人物与不同图谱实例的圆形裁切标识互不冲突。

## 变更文件

- `web/src/components/GraphPortrait.tsx`：圆形肖像、预设头像和图片失败处理。
- `web/src/components/CharacterGraph.tsx`：替换姓氏圆点，保留既有人物详情及图谱操作。
- `web/src/styles.css`：头像边框与玩家标记。
- 本记录及 `docs/next-development-plan-2026-09-15.md`：更新完成边界与后续工作。

## 验证

- `npm --prefix web test`：14 项通过。
- `npm --prefix web run build`：TypeScript 检查与 Vite 构建通过。
- `git diff --check`：通过。
- 实际 React 页面搭配只读 API（测试端口 5174/8001）打开既有顾长离存档，陆照临与顾长离肖像显示正常；鼠标点击陆照临、键盘 Enter 打开顾长离人物卡，均进入原有详情弹窗。
- 深色和浅色桌面主题完成视觉检查。拖动陆照临后位置更新，且不误开人物卡；放大到 1.2 倍及复位回原坐标通过。
- 临时组件检查页验证 8 种预设、错误图片 URL、错误 URL 更换为有效肖像、清空引用、缺图时点击回调；9 个头像裁切 ID 唯一，错误图片被移除后仍保留预设头像。临时页面已移除。

截图：[实际图谱深色](evidence/c2-graph-portraits-2026-09-15/real-dark.png)、[实际图谱浅色](evidence/c2-graph-portraits-2026-09-15/real-light.png)、[预设与失败兜底](evidence/c2-graph-portraits-2026-09-15/fallbacks.png)。这些是界面验证，不作为新增模型叙事质量验收；没有调用剧情或图像生成。

## 下一步

补充完成：详细身份卡也复用同一头像组件，以 96px 圆形头像替换姓氏圆标，保留姓名、身份简介和详情交互。只读桌面页面验证陆照临与顾长离切换时，卡片头像同步更新；构建与差异检查通过。见 [详细身份卡截图](evidence/c2-graph-portraits-2026-09-15/detail-card-light.png)。改动为 `SessionPage.tsx` 和对应头像尺寸、边框样式。

C 第 3 步已根据 C1 的公开状态证据，增加死亡灰度、离队等状态表达，并核对历史与兄弟分支切换，见 [C3 验收记录](c3-character-status-acceptance-2026-09-15.md)。只开发桌面端。
