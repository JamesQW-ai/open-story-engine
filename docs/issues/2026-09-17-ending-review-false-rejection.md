问题编号：END-001

状态：未解决。归入 NQ-001 的审查稳定性后续专项，不阻塞可独立验证的请求恢复和用量统计；阻塞三类自然终局的稳定可用性验收。

## 本次证据

模型 `deepseek-v4-pro:cloud`，使用当前终局提示及真实提案入口。七个对照样本中，正常结局通过，四个反例被拒绝，但预先定义为正例的偏离与失败结局未通过。没有传输失败、格式失败或自动重试。

- `deviation_abandoned` 返回 `unknown`：正文明确写出放下两项目标、走回山脚、完成退场和不再承诺行动，模型仍认为没有改变后的实际结果。它承认问题被有依据放下，却又以证据不够充分否定问题交代。
- `failure_irreversible` 返回 `reject`：正文写出救助失败、查证途径消失、善后完成与停止路线，输入也带有每项目标／问题的 `abandoned` 状态及原文证据。模型仍称缺少账本依据，并把如实留空的答案当作必须继续处理的冲突。

原始输入、响应与检查理由见[真实模型证据目录](../evidence/live-ending-review-2026-09-17/report.json)，分析与边界见[验证记录](../live-ending-review-2026-09-17.md)。样本是官方长篇背景下的人工隔离夹具，不是已经通过完整事实审查的连续游玩正文。

## 已确认与待验证

已确认的是模型结论与预登记样本预期不符，以及部分理由忽略了输入中的明确证据。尚未证明模型版本、提示词、账本投影或样本表述中哪个因素是主因。

可能的混淆包括：把“结束这条路线”理解成“所有未知都必须获得答案”；把目标 `completion.met=false` 理解成所有结局都不允许成立，而该字段本来只代表目标未达成。此为待验证假设，不能据此直接删除证据字段、强制允许 `abandoned` 或跳过语义审查。

## 处理边界

本轮保留原代码和提示，不扩写样本、不连续重试到通过、不将模型 `unknown` 当作批准。后续集中质量专项应固定样本及预期，比较账本语义表达和模型表现，持续保留四类反例；不能以放宽结局守卫换取正例通过。完整验收仍需真实连续路线，不能只依靠这七条短样本。
