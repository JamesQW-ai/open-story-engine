本轮按用户授权调用真实模型，验证可独立判定的功能契约；不把真实模型返回的正文当作叙事质量验收，也不写入正式存档。

## 范围

自动清单当前只有《太虚遗录》第一部，`taixu-relics-part1@0.1.2`，102610 CJK。检查使用官方开局夹具提供的权威上下文，真实模型通过生产规划、方向验证和流式取消路径；没有调用已停用的《雨夜候车室》。

## 结果

模型为 `deepseek-v4-pro:cloud`，预算最多 6 次，实际 4 次供应商调用，报告 token 为 17085；取消调用没有用量回执，不能据此推断费用。

| 检查 | 结果 | 耗时 |
| --- | --- | ---: |
| 明确受伤结果规划 | 通过 | 5707 ms |
| 放弃旧目标并建立自身安全目标 | 通过 | 7607 ms |
| 根据当前状态生成 2～4 个后续方向 | 通过 | 3030 ms |
| 首段流式数据后取消且不写入正式分支 | 通过 | 1139 ms |

四项均通过。取消路径返回 `draft_cancelled`，工作线程停止，未创建正式分支。原始报告和结构化模型产物保存在 [证据目录](evidence/live-function-contracts-2026-09-17/)。

## 验收边界

本轮证明受伤、目标变更、动态方向数量和流式取消的程序契约在一次真实调用中可通过。它不证明普通场景正文长度、连续三类场景、人物经历连续性、审查漏报或局部修复质量；NQ-001、END-001、SRC-001 继续暂缓。未修改正式存档、冻结母本或生产图片。

复跑入口：

```bash
.venv-api/bin/python -B -m test_support.live_functionality \
  --contracts-only --max-calls 6 --output /tmp/ose-live-contracts-new
```
